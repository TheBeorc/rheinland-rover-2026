# Aran Islands — Placeholder Source

Replace this file (and add more `.md` / `.txt` files alongside it) with the
real source documents you want the assistant to cite. Every file in this
folder is loaded at request time and injected into the system prompt with its
filename as the citation label.
